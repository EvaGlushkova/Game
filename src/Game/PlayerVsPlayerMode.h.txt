/*
Glushkova Eva st108255@student.spbu.ru
*/


#ifndef PLAYERVSPLAYERMODE_H
#define PLAYERVSPLAYERMODE_H

#include "GameMode.h"

/**
 * @class PlayerVsPlayerMode
 * @brief Two human players mode
 */

class PlayerVsPlayerMode : public GameMode {
public:
    /**
     * @brief Initialize two human players
     * @param deck Game deck
     */
    void initialize(Deck& deck) override;
    
    /**
     * @brief Get mode name
     * @return "Player vs Player"
     */
    std::string getModeName() const override;
};

#endif 