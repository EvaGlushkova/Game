/*
Glushkova Eva st108255@student.spbu.ru
*/

#ifndef EASY_AIPLAYER_H
#define EASY_AIPLAYER_H

#include "AIPlayer.h"

/**
 * @class EasyAIPlayer
 * @brief AI player with simple strategy
 */

class EasyAIPlayer : public AIPlayer {
public:
    /**
     * @brief Constructor
     * @param name Player name
     */
    EasyAIPlayer(const std::string& name = "EasyAI");
    
    /**
     * @brief AI chooses card with 75% chance
     * @return Card index to play, or -1 to pass
     */
    int chooseCardToPlay() override;
    
    /**
     * @brief Get specific AI info
     */
    std::string getPlayerInfo() const override;
};

#endif 